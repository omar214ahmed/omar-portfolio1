# Omar Portfolio (Vite + React)

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy on Vercel
Import this repo in Vercel. It will detect Vite automatically.

- Build Command: `npm run build`
- Output Directory: `dist`
